import backGroundImage from '../../assets/images/surface1.jpg';

const homePageStyles = {
  wrapper: {
    height: `calc(100vh - 50px)`,
    backgroundImage: `url(${backGroundImage})`,
  },
};

export default homePageStyles;
